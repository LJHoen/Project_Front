import {Order} from './order';

export class User {
  id: string;
  username: string;
  password: string;
  firstName: string;
  lastName: string;
  address: string;
  bankAccount: string;
  token: string;

  /*constructor(id: string, username: string, password: string, firstName: string,
              lastName: string, address: string, bankAccount: string, token: string) {
    this.id = id;
    this.username = name;
    this.password = password;
    this.firstName = firstName;
    this.lastName = lastName;
    this.address = address;
    this.bankAccount = bankAccount;
    this.token = token;
  }*/
}
